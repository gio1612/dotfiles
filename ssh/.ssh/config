# ~/.ssh/config
# Routes SSH through the 1Password agent socket (bridged from Windows via socat+npiperelay)

Host *
    IdentityAgent ~/.ssh/agent.sock
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host github.com
    HostName github.com
    User git
    IdentityAgent ~/.ssh/agent.sock
